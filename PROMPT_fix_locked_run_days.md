## Summary (build 7.43)

**Which case it was: neither of the two offered — and the diagnosis was run before
anything was built, as asked.** Real cell flags printed from the actual generator against
Billy's stored plan shape (5 days/week, long run Saturday, 4 lift days), with today being
Wed 5 Aug 2026:

| Day | miles | before | after | result |
|---|---|---|---|---|
| Mon 3 Aug | 0 | **true** | false | no inputs |
| **Tue 4 Aug** | 0 | **true** | false | **no inputs** ← the reported cell |
| Wed 5 Aug | 3 | false | false | loggable |
| Thu 6 Aug | 0 | false | false | no inputs |
| Fri 7 Aug | 3 | false | false | loggable |

The cause is `c.before`, not `c.miles === 0`. `buildRunWeeks()` sets
`before: date < block.start` (index.html:6036), and because the plan is regenerated from
today, **week 0's `block.start` is today** — so every earlier weekday of the current week
fails `!c.before` and renders with no inputs. On a Wednesday, Monday and Tuesday of this
week are dead. That is exactly "a Tuesday this week". "A Tuesday last week" is a third
thing again: last week is not in `plan.weeks` at all, which is the rolling-window case the
missed-run control already covers.

Worth recording because it contradicts an earlier build's stated premise:
`PROMPT_log_past_runs.md` asserted that "within the Run tab's current week, a day that
already passed is not blocked… Monday's run is already loggable on Thursday of the same
week. That part works." **It does not** — `c.before` blocks it. That claim was wrong and is
what left this hole open.

**So it was not a flag bug to fix.** `before` is correct for *planning* — the plan starts
today and cannot schedule yesterday — and the generator is out of scope by standing rule.
The flags are untouched (asserted: both `before: date < block.start` and the `loggable`
expression are unchanged in the file). What was missing was any route from a dead cell to
the logging that already worked for it.

**What changed:** every dead cell up to and including today now renders one small link —
`+ log a run anyway`, or `edit logged run` if that date already has one. It calls
`logRunAnyway(key)`, which force-opens the existing missed-run control, sets its date input
to that cell's own date, and hands off to `onMissedDatePick()`. From there it is the same
`runLogInputsHTML()`, the same `onRunLogEdit`/`onRunLogCommit`, the same
`runLogs`/`persistRunLogs()`. No new logging mechanism, no new storage — a discoverability
fix, which is what the task asked for. It covers all three dead-cell causes uniformly
rather than only the unscheduled one.

Future cells deliberately get nothing: you still cannot log a run you have not done.

**Confirmation it saves and survives a reload:** `sets.js` section 16 builds the real plan,
finds the actual before-today cells, asserts they had no inputs and now report as
backfillable, drives `logRunAnyway()` on one, checks the control opened prefilled with that
date, writes 5.5 miles and feel 8 through the shared commit, confirms it reached
`monk_run_logs_v1`, then reloads via `loadRunLogs()` and reads 5.5 and 8 back. It also
confirms a genuinely scheduled day keeps its normal inline inputs and is *not* given a link
instead, that future cells offer nothing, and that a fully out-of-window date still works
through the original control.

Suites: `check.js` both blocks parse, `onb.js` 71/0, `compat.js` 86/0, `sets.js` 229/0
(23 new), CSS balanced.

**Not seen in a browser.** The extension wedged again before this could be loaded. The
logic is covered against the real generator, but the link's size and placement inside a
schedule cell — which is a layout judgement — has not been looked at.

---

## Sync first
```
del ".git\index.lock" 2>nul
del ".git\ORIG_HEAD.lock" 2>nul
git fetch origin
git reset --hard origin/master
```
Confirm `git log --oneline -3` before starting. HEAD should include whatever build `PROMPT_collapse_run_setup.md` produced or newer.

## Context
This replaces an earlier draft of this same prompt. Billy tried the "+ Log a missed run" control from a prior build, confirmed it technically works, and rejected it as the fix anyway: **he does not want a separate control at all.** His words: "I don't want a new input tab on a run. I want it in the calendar section, and it never to go blank if a day passes. I want it to be able to be adjusted no matter what day it is." That's the actual spec, direct from him, not a proposal, follow it as written.

**Where "the calendar section" actually has to be, structurally, and why the Run tab's own schedule cannot be it:** the Run tab's weekly schedule (`runWeekHTML()`, index.html:5996) is generated by `generateRunPlan()`, which calls `buildRunWeeks(today, race)` and rebuilds from today forward every time, "the plan is always regenerated from today, so it has no past weeks" (comment already in the file). That's load-bearing for the adaptive plan system and a prior build deliberately did not touch it. A day from a week that's fully elapsed simply has no cell there anymore, no amount of un-gating fixes that, there's nothing to un-gate.

The **Calendar tab** (`renderCalendarTab()`, index.html:6557 area, `#tab-calendar`) is different: it draws all 10 training weeks by fixed date, past, present, and future, and it doesn't disappear historical weeks the way the Run tab's rolling generator does. This is the one place in the app structurally capable of "never going blank no matter what day it is." Its run detail currently comes from `calRunCells(monday)`, which calls `generateRunPlan(readRunInputs(), startOfToday())` and filters to the week being drawn, so for an elapsed week this returns null and past weeks render lift days with no run information and no inputs at all.

A prior build looked at exactly this option and talked itself out of it: "putting log inputs on all seven days of every past week is clutter rather than the low-key affordance the task asked for." Billy is now explicitly overriding that trade-off. He wants every day's fields present, whether or not a run was scheduled there, whether or not it's fully in the past. Completeness beats minimalism for this specific feature. Don't re-derive the old "keep it low-key" conclusion, it's been superseded by a direct instruction.

## Task
1. **Give every day cell on the Calendar tab the same four log fields already built** (`runLogInputsHTML()` markup, `onRunLogEdit`/`onRunLogCommit` handlers, `runLogs`/`persistRunLogs()` storage, `saveRunLog()`), regardless of:
   - whether that day was a scheduled run day in whatever the rolling plan generator currently thinks (don't gate on `c.miles > 0`)
   - whether the week is in the past, current, or future relative to today
   No new logging mechanism, this is the exact same four fields and the exact same storage every other part of this app already uses for run logs, just no longer conditionally hidden.
2. **The only gate that should remain is "not in the future."** You still cannot log a run you have not done yet, that rule is sound and Billy has not objected to it, keep blocking dates after today.
3. **Stop relying on `generateRunPlan()`/`calRunCells()` to decide whether a day is loggable.** Since that generator has no memory of elapsed weeks, it cannot be the source of truth for which days on this tab get inputs. The Calendar tab already computes real fixed dates for its 10 weeks independent of the rolling generator (that's how it draws past weeks by date at all), key the log fields off that real date directly, not off whatever the regenerated plan currently contains.
4. **The "+ Log a missed run" control on the Run tab becomes redundant once this lands**, since the Calendar tab now covers everything it was compensating for. Don't rip it out as part of this task, that's Billy's call to make separately, but say so plainly in the summary so he can decide.
5. Don't touch `generateRunPlan()`, `buildRunWeeks()`, or the Run tab's own rolling schedule and its existing gating, those are unrelated to this fix and already correct for what they're for.

## Verify before pushing
1. Pick a date from a week that has fully elapsed, well outside the Run tab's current rolling window, on the Calendar tab specifically. Confirm it shows the four log fields, log a value, reload the page from scratch, confirm it persisted.
2. Pick a day that was never a scheduled run day anywhere in the plan (a pure lift or rest day). Confirm it also shows the four log fields on the Calendar tab and can be logged, since Billy explicitly wants every day adjustable, not just the ones the generator happened to schedule a run on.
3. Confirm a future date still correctly refuses logging, that rule stays.
4. Confirm the Run tab's own rolling schedule and its existing "+ Log a missed run" control still function exactly as before, unchanged, even though they're now redundant with the Calendar tab.
5. No console errors, both script blocks parse, all existing suites pass, plus new coverage proving a fully-elapsed, never-scheduled day on the Calendar tab is loggable and persists.

## Last steps
Bump the build stamp. Commit with a clear message. Confirm `git log --oneline --graph -5` is a clean line, push, and confirm with `git log --oneline origin/master -3` that the push landed.

Leave a short summary at the top of this file when done: confirm every Calendar tab day (past, unscheduled, or both) now carries working log inputs, confirm future dates are still blocked, and flag directly that the Run tab's missed-run control is now redundant so Billy can decide whether to remove it.
